services:
  config-server:
    build: .
    container_name: config-server
    ports:
      - "8762:80"
    networks:
      tucan-net:
        aliases:
          - config-server
    deploy:
      resources:
        limits:
          memory: 400M

networks:
  tucan-net:
    external: true